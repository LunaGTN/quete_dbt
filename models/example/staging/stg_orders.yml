version: 2

models:
    - name: stg_orders
      columns:
        - name: orders_id
          tests: 
          - not_null
          - unique
        - name: orders_customer
        - name: ordered_at
        - name: store_id
          tests:
          - not null